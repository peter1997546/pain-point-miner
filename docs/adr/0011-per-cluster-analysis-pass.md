# Per-cluster Analysis Pass

Analysis Pass must process one Candidate Cluster / Pain Point at a time. Multi-agent fan-out across clusters is fine. Feeding the entire crawled corpus (or all clusters at once) into a single AI analysis step is out of scope — it defeats the Script + Skill split and recreates context overload.

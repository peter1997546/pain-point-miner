# Discovery over restriction

The Miner’s job is to find and analyze real Pain Points from its Source Catalog. Intent / Theme are optional steers, not a restriction sheet whose purpose is to force the result set down to a handful of hits. An empty Intent must still be a valid run (Script/CLI input). That validity does not excuse skipping the Skill Intent interview — see ADR-0017.
